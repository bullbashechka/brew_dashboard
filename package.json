{
  "name": "brew-dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "webapp",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd webapp dev",
    "prepare": "husky",
    "audit": "node scripts/audit.mjs",
    "security:scan": "node scripts/security-scan.mjs",
    "validate:stage12": "bun run lint && bun run typecheck && bun run db:check && bun run test && bun run test:integration:docker && bun run test:e2e && bun run test:e2e:system:docker && bun run build && bun run audit && bun run security:scan",
    "lint": "bun run check:workspace && bun run --cwd packages/contracts lint && bun run --cwd backend lint && bun run --cwd webapp lint && bun run format:check",
    "format": "prettier --write \"**/*.{js,mjs,cjs,ts,tsx,json,jsonc,css,html}\"",
    "format:check": "prettier --check \"**/*.{js,mjs,cjs,ts,tsx,json,jsonc,css,html}\"",
    "check:workspace": "node scripts/check-active-workspace.mjs",
    "check:build-secrets": "node scripts/build-artifact-security.mjs check",
    "typecheck": "bun run --cwd packages/contracts typecheck && bun run --cwd backend typecheck && bun run --cwd webapp typecheck",
    "test": "bun run --cwd packages/contracts test && bun run --cwd backend test:unit && bun run --cwd webapp test && bun test scripts/build-artifact-security.test.ts",
    "test:integration": "E2E_ACCOUNT_KIND=e2e bun run --cwd backend test:integration",
    "test:integration:docker": "docker compose up -d --wait postgres-test && DATABASE_TEST_ADMIN_URL='postgresql://postgres@127.0.0.1:54329/postgres' bun run test:integration",
    "test:e2e": "bun run --cwd webapp test:e2e",
    "test:e2e:system": "E2E_SYSTEM=1 E2E_ACCOUNT_KIND=e2e bun run --cwd webapp test:e2e",
    "test:e2e:system:docker": "docker compose up -d --wait postgres-test && DATABASE_TEST_ADMIN_URL='postgresql://postgres@127.0.0.1:54329/postgres' bun run test:e2e:system",
    "test:load:release": "node scripts/release-load.mjs",
    "db:generate": "bun run --cwd backend db:generate",
    "db:check": "bun run --cwd backend db:check",
    "db:migrate": "bun run --cwd backend db:migrate",
    "db:smoke:hyperdrive": "bun run --cwd backend db:smoke:hyperdrive",
    "admin:create-user": "bun run --cwd backend admin:create-user",
    "admin:reset-password": "bun run --cwd backend admin:reset-password",
    "admin:disable-user": "bun run --cwd backend admin:disable-user",
    "admin:delete-user": "bun run --cwd backend admin:delete-user",
    "admin:cleanup-events": "bun run --cwd backend admin:cleanup-events",
    "build": "bun run --cwd packages/contracts build && bun run --cwd backend build && bun run --cwd webapp build && bun run check:build-secrets"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "@types/bun": "^1.3.14",
    "eslint": "^10.0.0",
    "eslint-plugin-react-hooks": "^7.0.0",
    "eslint-plugin-react-refresh": "^0.5.0",
    "globals": "^17.0.0",
    "husky": "^9.1.7",
    "prettier": "^3.7.0",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.0.0"
  }
}
