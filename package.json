{
  "name": "brew-dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "webapp",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd webapp dev",
    "lint": "bun run check:workspace && bun run --cwd packages/contracts lint && bun run --cwd backend lint && bun run --cwd webapp lint && bun run format:check",
    "format": "prettier --write \"**/*.{js,mjs,cjs,ts,tsx,json,jsonc,css,html}\"",
    "format:check": "prettier --check \"**/*.{js,mjs,cjs,ts,tsx,json,jsonc,css,html}\"",
    "check:workspace": "node scripts/check-active-workspace.mjs",
    "check:client-secrets": "node scripts/check-client-bundle.mjs",
    "typecheck": "bun run --cwd packages/contracts typecheck && bun run --cwd backend typecheck && bun run --cwd webapp typecheck",
    "test": "bun run --cwd packages/contracts test && bun run --cwd backend test:unit && bun run --cwd webapp test",
    "test:integration": "bun run --cwd backend test:integration",
    "test:e2e": "bun run --cwd webapp test:e2e",
    "build": "bun run --cwd packages/contracts build && bun run --cwd backend build && bun run --cwd webapp build && bun run check:client-secrets"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "@types/bun": "^1.3.14",
    "eslint": "^10.0.0",
    "eslint-plugin-react-hooks": "^7.0.0",
    "eslint-plugin-react-refresh": "^0.5.0",
    "globals": "^17.0.0",
    "prettier": "^3.7.0",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.0.0"
  }
}
